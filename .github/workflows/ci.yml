name: CI

# cfbwrite writes protection-relay settings files. Its self-verification -- it
# reopens what it built and compares every stream against the source before
# handing it over -- is the reason it is allowed to. That property is what this
# job exists to keep true.

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        # 3.10 is the floor `requires-python` declares; 3.14 is the newest
        # release. Every version in between is run because a hole is where a
        # real incompatibility hides.
        python-version: ["3.10", "3.11", "3.12", "3.13", "3.14"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"
      - name: Lint
        run: python -m ruff check .
      - name: Types
        run: python -m mypy
      - name: Tests
        run: python -m pytest tests/ -q

  package:
    # The package is on PyPI, so broken metadata would otherwise surface at
    # upload time -- after the version number has already been spent, since a
    # PyPI version can be yanked but never reused.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Build and check
        run: |
          python -m pip install --upgrade pip build twine
          python -m build
          python -m twine check dist/*
      - name: The wheel must carry py.typed and the licence
        # py.typed is what makes the annotations visible to a consumer's mypy;
        # without it the library is untyped no matter how well it is annotated.
        run: |
          python - <<'PY'
          import glob, zipfile, sys
          names = zipfile.ZipFile(glob.glob("dist/*.whl")[0]).namelist()
          missing = [w for w in ("cfbwrite/py.typed", "LICENSE")
                     if not any(w in n for n in names)]
          sys.exit(f"missing from the wheel: {missing}" if missing else 0)
          PY
